import React from 'react';

const CommunityLeftBar = () => {
    return (
        <div className="hidden lg:block">
            {/* Left bar is currently empty, but keeps the layout balanced */}
        </div>
    );
};

export default CommunityLeftBar;
